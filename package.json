{
  "name": "qa-ui-cypress",
  "version": "1.0.0",
  "private": true,
  "description": "Cypress end-to-end test framework with page abstractions, validated runtime configuration, and CI diagnostics.",
  "engines": {
    "node": ">=22 <23 || >=24 <25"
  },
  "allowScripts": {
    "cypress": true
  },
  "scripts": {
    "test": "cypress run",
    "test:chrome": "cypress run --browser chrome",
    "test:firefox": "cypress run --browser firefox",
    "test:electron": "cypress run --browser electron",
    "test:login": "cypress run --spec cypress/e2e/login.cy.js",
    "cypress:open": "cypress open",
    "cypress:verify": "cypress verify",
    "config:check": "node config/runtime.selftest.js && node config/runReporter.selftest.js && node config/cypressConfig.selftest.js && node config/retryPolicy.selftest.js",
    "cypress:install": "cypress install"
  },
  "license": "MIT",
  "devDependencies": {
    "cypress": "^15.21.1"
  },
  "packageManager": "npm@11.19.1"
}
